export enum ResourceType {
  room = 'room',
  desk = 'desk',
  bathroom = 'bathroom',
};

export enum ResourceStatus {
  pending = 'pending',
  approved = 'approved',
  rejected = 'rejected',
  deleted = 'deleted',
};

export enum ScheduleStatus {
  pending = 'pending',
  approved = 'approved',
  rejected = 'rejected',
};